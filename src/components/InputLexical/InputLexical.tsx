import React from "react";

const InputLexical = ({
  name,
  label,
  placeholder,
  IconFirstComponent,
  disabled,
  helperText,
  required,
  control,
}: {
  name: string;
  label?: string;
  placeholder?: string;
  disabled?: boolean;
  helperText?: string;
  required?: boolean;
  IconFirstComponent?: any;
  control: any;
}) => {
  return <div>InputLexical</div>;
};

export default InputLexical;
